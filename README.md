# Sprint_6

Автотесты для сервиса аренды самокатов.

Установить зависимости — pip install -r requirements.txt
Запуск тестов - pytest tests -v
Сгенерировать allure отчет - pytest tests --alluredir=allure_results
Открыть отчет: - allure serve allure_results
